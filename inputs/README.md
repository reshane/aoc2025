# Inputs

These files contain the puzzle inputs. They are .gitignored per the advent of code guidelines.
To solve for an input it must be named `{day_num}.txt` where `day_num` is at least two digits (use leading zero for single digit days).
